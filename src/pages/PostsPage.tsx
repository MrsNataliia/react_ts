import React, {FC} from 'react';

import {Posts} from "../components/PostsConteiner/Posts";

const PostsPage: FC = () => {
    return (
        <div>
            <Posts/>
        </div>
    );
};

export {PostsPage};